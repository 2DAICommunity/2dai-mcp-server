{
  "name": "2dai-mcp-server",
  "version": "1.1.1",
  "description": "MCP server for 2DAI — let Claude, Cursor, Cline and Windsurf generate images and video, upload references and organise your cloud drive on your own 2DAI account.",
  "type": "module",
  "bin": {
    "2dai-mcp-server": "dist/index.js"
  },
  "main": "dist/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/2DAICommunity/2dai-mcp-server.git"
  },
  "homepage": "https://2dai.io",
  "bugs": {
    "url": "https://github.com/2DAICommunity/2dai-mcp-server/issues"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "lint:stdout": "! grep -rnE 'console\\.(log|info|debug|dir|table)\\(' src/ || (echo 'stdout write in src/ corrupts the stdio JSON-RPC stream' && exit 1)",
    "prepublishOnly": "npm run lint:stdout && npm run build"
  },
  "keywords": [
    "mcp",
    "modelcontextprotocol",
    "2dai",
    "image-generation",
    "video-generation",
    "ai"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "2dai-cloud-sdk": "^2.3.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.3"
  }
}
